<%= form_with(model: school_attachment, local: true) do |form| %>
  <% if school_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_attachment.errors.count, "error") %> prohibited this school_attachment from being saved:</h2>

      <ul>
      <% school_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :school_id %>
    <%= form.number_field :school_id, id: :school_attachment_school_id %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.text_field :avatar, id: :school_attachment_avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
