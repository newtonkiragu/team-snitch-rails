<%= image_tag @teacher_attachment.avatar %>
<%= form_with(model: teacher_attachment, local: true) do |form| %>
  <% if teacher_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_attachment.errors.count, "error") %> prohibited this teacher_attachment from being saved:</h2>

      <ul>
      <% teacher_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.number_field :teacher_id, id: :teacher_attachment_teacher_id %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.text_field :avatar, id: :teacher_attachment_avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
